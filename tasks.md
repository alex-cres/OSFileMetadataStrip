1. ~~alternatives to not have the SixLabors.ImageSharp package~~ ✓ done
2. ~~make a O11 version of the component as well~~ ✓ done
3. ~~review any stale documentation~~ ✓ done
4. ~~Add support for audio files (MP3, WAV, FLAC, OGG, AAC)~~ ✓ done
5. ~~Add support for video files (MP4, MOV, AVI, MKV, WebM)~~ ✓ done
6. ~~Fix animated GIF and multi-frame TIFF support — switch StripImageMetadata from MagickImage to MagickImageCollection so all frames are preserved after stripping~~ ✓ done
7. ~~Strip PDF XMP metadata streams (catalog /Metadata entry) in addition to the /Info dictionary~~ ✓ done
8. ~~Strip OOXML app properties (docProps/app.xml) and custom properties (docProps/custom.xml)~~ ✓ done
9. ~~Graceful handling of encrypted/password-protected PDFs and OOXML files — return original unchanged with a processingError audit note instead of throwing~~ ✓ done
10. ~~Investigate stripping author names from tracked changes and comments inside OOXML document bodies~~ ✓ done (implemented as opt-in via StripBodyAuthors parameter)
11. ~~Strip SVG-native XML metadata — `<title>`, `<desc>`, and Dublin Core RDF `<metadata>` elements are plain text nodes that survive `Strip()` today; any tool that extracts SVG text for AI context would expose them verbatim.~~ ✓ done (dedicated SVG XML strip path removes all three at every depth; extracted values recorded in `ExtractedMetadata`)
12. ~~Strip EPUB metadata — EPUB is a ZIP with an OPF manifest containing `dc:description`, `dc:creator`, `dc:rights`, etc. as plain text; document-processing pipelines that unzip EPUB before sending to AI include these fields in the text context. Currently mis-classified as OpenXml and left untouched.~~ ✓ done (ZIP mimetype dispatcher routes EPUB to `StripEpubMetadata`, which follows `META-INF/container.xml` to the OPF and blanks every `dc:*` and OPF `<meta>` element)
13. ~~Remove OOXML embedded thumbnail (`docProps/thumbnail.jpeg`) — Office apps embed a rendered page preview inside the ZIP; if the document contained injected text in a visible area the thumbnail carries that image to a vision model.~~ ✓ done (thumbnail part and its `_rels/.rels` relationship removed in the DOCX/XLSX/PPTX post-process stage)
14. Parse and report XMP content — images and PDFs with XMP currently report `"xmp": "present"` in `ExtractedMetadata`; parse the XMP XML to surface the actual field values (dc:creator, xmp:CreateDate, etc.) so the audit log is meaningful.
15. Add `ExtractFileMetadata` read-only action — exposes the existing extraction logic without stripping; useful for audit/preview workflows where the caller wants to inspect what metadata a file contains before deciding to strip.
16. ~~Extend iPhone photo testing (HEIC/HEIF) — HEIC/HEIF is the default format for iPhone cameras and carries significant EXIF metadata; Magick.NET is expected to handle it but there are no dedicated tests and the format is not documented in the README.~~ ✓ done (AVIF: full strip round-trip with 8 tests each platform; HEIC/HEIF all brands: detection routing fixed, metadata stripped in memory, output transcoded to JPEG via `MagickMissingDelegateErrorException` fallback with `transcodedFormat` audit key — x265 HEVC encoder is GPL-licensed and cannot be bundled; both documented in README)
17. ~~Extend format coverage for 40+ Priority 1/2/3 image formats (JXL, JP2, PSD, TGA, EXR, HDR, DDS, MNG, etc.) — write test files per the filemetadatastrip-test-coverage skill.~~ ✓ done (43 new test files across both ODC and O11 test projects; TGA/ICO/XCF/DCM detection fixes; HDR encoder-comment stripping; APNG `acTL` detection with format hint; empty-collection guard; broader decode exception handling)
18. ~~Strip DPX/CIN `cin:*` production attributes — file.filename, origination.device, film.id, etc. survive `Strip()` today and appear in the output DPX/CIN.~~ ✓ done (`dpx:*` and `cin:*` per-image attributes explicitly removed after `Strip()` in the image path; values captured in `ExtractedMetadata` under `dpx` / `cin` keys before being cleared)
19. ~~Fix ORA (Open Raster) mis-classification — ZIP with `mimetype = image/openraster` is currently routed to `StripOpenXmlMetadata()` (docProps/ absent) and returned unchanged; needs a dedicated ORA path that strips `stack.xml` creator/description.~~ ✓ done (ZIP mimetype dispatcher routes ORA to `StripOraMetadata`, which blanks `name` and `description` attributes on every element in `stack.xml` while preserving structural attributes)
20. ~~Add RTF (Rich Text Format) support — `\author`, `\company`, `\operator` etc. inside the `\info` group survive today because RTF is not routed to any strip pipeline.~~ ✓ done (dedicated `StripRtfMetadata` scans the file as ISO-8859-1 and blanks every string-bearing `\info` control-word group via a compiled regex; numeric control words preserved; 33 tests per platform)
21. ~~Extend audio detection to AIFF / AIFC, APE (Monkey's Audio), WavPack (`.wv`) and MPC (Musepack SV7 + SV8) — TagLibSharp already parses each, only detection branches in `DetectCategory` were missing.~~ ✓ done (magic-byte detection + `GetMediaExtensionHint` returns `.aiff` / `.ape` / `.wv` / `.mpc`; 17 detection-contract tests per platform in `ExtendedAudioDetectionTests.cs`, including false-positive guards for bare `MP+` and `FORM` + `ILBM`)
22. ~~Lock in HTML / HTM passthrough contract via a dedicated test file so any future reclassification has to update the tests deliberately.~~ ✓ done (`HtmlPassthroughTests.cs`, 7 tests per platform; asserts `IsPassthrough = true`, `CleanFile == input`, `RemovedEntryCount = 0`, `ExtractedMetadata == "[]"`, and that `<meta name="author">` + `<title>` survive unchanged in the output)