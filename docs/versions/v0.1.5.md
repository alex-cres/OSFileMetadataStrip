# v0.1.5

**Released:** 2026-07-30  

**Forge ODC:** https://www.outsystems.com/forge/component-overview/25245/filemetadatastripping-odc

**Forge O11:** https://www.outsystems.com/forge/component-overview/25314/filemetadatastripping-o11

---

## Added

- **Flat ODF (`.fodt` / `.fods` / `.fodp`)** — single-file XML variant of ODF — is now detected and routed to a dedicated strip path. Detection matches the `<office:document>` root element in the OASIS office namespace (`urn:oasis:names:tc:opendocument:xmlns:office:1.0`); files are parsed with `XDocument.Load` and the existing `ExtractAndClearOdfMetadata` helper is reused to blank every `dc:*` / `meta:*` child of the `<office:meta>` block (dc:creator, dc:title, dc:description, dc:subject, meta:initial-creator, meta:generator, meta:editing-cycles, meta:editing-duration, meta:user-defined). The output remains a valid Flat ODF XML document. No new NuGet dependency (reuses `System.Xml.Linq`). Verified with 22 tests per platform (44 total) covering detection, per-field strip for each of the three Flat ODF extensions, extraction / count parity with ZIP-based ODF, clean baseline, format validity (XML declaration + `<office:document>` root preserved), security invariant, IsPassthrough contract, and edge cases (missing `<office:meta>` block, malformed XML).

- **Word 2003 XML (WordProcessingML — `.xml`)** is now detected and routed to a dedicated strip path. Detection matches the `<w:wordDocument>` root element in the WordProcessingML namespace (`http://schemas.microsoft.com/office/word/2003/wordml`). Strips every child of `<o:DocumentProperties>` (Author, LastAuthor, Company, Manager, Title, Subject, Keywords, Description, Category, Template, HyperlinkBase, Application, AppVersion, TotalTime, LastPrinted, Created, LastSaved, Pages, Words, Characters, CharactersWithSpaces, Lines, Paragraphs, revision counters), removes every child of `<o:CustomDocumentProperties>`, and when `StripBodyAuthors = True` also blanks the `w:author` and `aml:author` attributes on tracked-change and comment elements throughout the document body. Removed values are captured in `ExtractedMetadata` under `documentProperties`, `customDocumentProperties`, and `bodyAuthors` (the last one populated only when body-author stripping is enabled). The output remains a valid Word 2003 XML document. No new NuGet dependency (reuses `System.Xml.Linq`). Verified with 30 tests per platform (60 total) covering detection, per-property strip, custom-property strip, body-author strip under both `StripBodyAuthors` settings, extraction / count parity, clean baseline, format validity, security invariant, IsPassthrough contract, and edge cases (missing DocumentProperties block, malformed XML, no author attributes to blank).

- **AIFF / AIFC, APE (Monkey's Audio), WavPack (`.wv`) and MPC (Musepack — SV7 and SV8)** are now detected in `DetectCategory` and routed to the existing TagLibSharp media pipeline. Detection magic bytes: AIFF/AIFC uses the `FORM` container + `AIFF` or `AIFC` form-type at bytes 8–11 (big-endian sibling of RIFF WAV); APE uses `MAC ` (4 ASCII bytes, trailing space); WavPack uses `wvpk`; MPC SV8 uses `MPCK`; MPC SV7 uses `MP+` followed by a stream-version byte whose low nibble is `0x07` (a strict check that rejects bare `MP+`-prefixed binary data as a false positive). `GetMediaExtensionHint()` returns the matching extension (`.aiff`, `.ape`, `.wv`, `.mpc`) so TagLibSharp picks the right parser. Verified with 17 detection-contract tests per platform (34 total) covering AIFF, AIFC, APE, WavPack, MPC SV7 and SV8 detection routing plus two false-positive guards (bare `MP+` prefix; `FORM` + `ILBM` legacy Amiga image).

- **HTML passthrough contract** is now locked in by a dedicated `HtmlPassthroughTests.cs` (7 tests per platform, 14 total). Documents that HTML is deliberately excluded from active metadata stripping: `<meta charset>`, `<meta viewport>`, `<meta http-equiv>` are functional and required for rendering, so selective meta-name stripping is fragile; and a metadata stripper that clears `<meta name="author">` while leaving `<script>` in place gives a false sense of security. HTML hardening belongs to a dedicated sanitiser (`HtmlSanitizer` / DOMPurify / Bleach) applied downstream by the caller. Tests assert `IsPassthrough = true`, `CleanFile == input`, `RemovedEntryCount = 0`, `ExtractedMetadata == "[]"`, and that `<meta name="author">` and `<title>` values survive unchanged in the output. Any future reclassification of HTML as an actively-stripped format has to update these tests deliberately.

- **RTF (Rich Text Format)** is now routed to a dedicated text-scanner strip path. Detection uses the 6-byte `{\rtf1` prefix (strict, to avoid false positives on ordinary `{`-prefixed binary data). The scanner reads the file as ISO-8859-1 (RTF is 7-bit ASCII on disk with `\'HH` hex escapes for non-ASCII, so Latin-1 preserves every byte 1:1), then blanks the string-bearing control-word groups inside the `\info` group via a compiled regex: `\author`, `\title`, `\subject`, `\keywords`, `\comment` (private/invisible), `\operator`, `\company`, `\doccomm` (visible "Comments" in Word Properties), `\category`, `\hlinkbase`, `\manager`. Numeric control words (`\version`, `\vern`, `\nofpages`, revision timestamps, edit-minute counters) are preserved because they are not user-controlled prompt-injection vectors. The control word itself is retained with an empty payload so the RTF structure remains well-formed. Removed values are captured in `ExtractedMetadata` under the control-word name; repeated occurrences of the same control word are preserved as a JSON array. No new NuGet dependency. Verified with 33 tests per platform (66 total) covering detection (`{\rtf1` prefix check, false-positive rejection for `{`-prefixed JSON and 5-byte `{\rtf`), per-field strip, extraction/count parity, clean baseline (`\info`-less RTF → `RemovedEntryCount = 0`, output byte-identical to input), format validity (`{\rtf1` prefix + trailing `}` preserved, body content preserved), security invariant (prompt-injection payloads in `\author` / `\comment` scrubbed), IsPassthrough contract, case-insensitive control-word matching (`\Author`), and graceful handling of a truncated 6-byte RTF (no throw, original returned unchanged).

- **Legacy binary Office (CFBF)** files are now routed to a dedicated strip path. Word 97–2003 (`.doc`, `.dot`), Excel 97–2003 (`.xls`, `.xlt`), and PowerPoint 97–2003 (`.ppt`, `.pot`, `.pps`) all share the same Compound File Binary Format container (magic `D0 CF 11 E0 A1 B1 1A E1`); detection uses that 8-byte magic (checked before the ZIP `PK\x03\x04` signature so there's no clash with OOXML/ODF/EPUB/ORA). The strip path wipes the two OLE property-set streams `\x05SummaryInformation` (Title, Subject, Author, Keywords, Comments, Template, Last-Saved-By, Application) and `\x05DocumentSummaryInformation` (Category, Manager, Company, ContentStatus, Language, custom properties). The container is consolidated after deletion so the freed sectors are dropped from the output — the raw property values do not survive in unallocated space. Known well-named properties are captured in `ExtractedMetadata` under `summaryInformation`, `documentSummaryInformation`, and `customProperties` before deletion for audit. Depends on `OpenMcdf 3.1.4` (MPL-2.0) and `OpenMcdf.Ole 3.1.4-experimental.1` (MPL-2.0). Verified with 29 tests per platform (58 total) covering detection, per-property strip, per-extension round-trip for all seven Office extensions, custom user-defined properties, extracted-metadata audit, format validity, clean-baseline (CFBF with no metadata streams → `RemovedEntryCount = 0`), security invariant, and edge cases (truncated / corrupt containers → `processingError` note, no throw).

- AVIF is now a fully supported image format: EXIF, IPTC, XMP, and comments are stripped and returned as `CleanFile`. Verified round-trip with Magick.NET.
- HEIC and HEIF (mif1/msf1 brands) are now correctly detected as images via ISOBMFF `ftyp` brand check (bytes 8–11). Metadata is extracted and recorded in `ExtractedMetadata`; the original file is returned with a `processingError` note because Magick.NET has no HEIC encode delegate on Windows.
- `IsHeifOrAvifBrand` private helper added to both ODC and O11 implementations to perform the ISOBMFF brand check.
- Comprehensive test coverage added for 40+ previously untested image formats across both ODC (`FileMetadataStripping.Tests`) and O11 (`FileMetadataStripping.O11.Tests`) test projects, covering: JXL (JPEG XL), JP2/J2K (JPEG 2000), PSD/PSB (Photoshop), TGA, APNG, DNG/CR2/NEF/ARW (RAW camera formats treated as TIFF), UHDR (Ultra HDR as JPEG), AI (Adobe Illustrator as PDF), DCM (DICOM), EXR (OpenEXR), HDR (Radiance RGBE), ORF/RAF/PEF/X3F (RAW camera), QOI (Quite OK Image), DDS (DirectDraw Surface), SVG, HEIF mif1/msf1 brands, XCF (GIMP), DPX/CIN (film formats), JXR/WDP (JPEG XR), MPO (Multi-picture Object), MNG (animated), PBM/PGM/PPM/PNM (Netpbm), PCX, SGI, SUN, PICT, XBM, XPM, JBIG, FITS, PCD/PCDS, ICO, WMF, WBMP, ORA (Open Raster), EPUB, and Animated WebP. Known pre-existing implementation gaps are documented inline in the relevant test files.
- **APNG fixed:** `IsApngFile()` detects APNG via the `acTL` chunk and passes `MagickFormat.APng` as a read hint to `MagickImageCollection`, ensuring all animation frames are decoded. Writing APNG still requires ImageMagick's `video` delegate (ffmpeg); without it `MagickMissingDelegateErrorException` triggers the existing JPEG fallback, and `ExtractedMetadata` receives a `transcodedFormat` note. `TestHelpers.CreateApng()` now produces real APNG bytes (with `acTL`, `fcTL`, `IDAT`, `fdAT` chunks) built from raw PNG chunk structures — no ffmpeg required for test data generation.
- `GenerateSamples` tool extended with sample files for TGA, ICO, XCF, DCM, and HEIF mif1.
- **SVG** files are now routed to a dedicated XML-aware strip path. `<title>`, `<desc>`, and `<metadata>` elements are removed at every depth (matched by local name, so unnamespaced children are also cleaned). The output remains a valid SVG.
- **EPUB** files are now routed to a dedicated strip path (via the ZIP `mimetype` entry) that follows the reference in `META-INF/container.xml` to the OPF package document, then blanks every Dublin Core element (`dc:creator`, `dc:title`, `dc:description`, `dc:rights`, …) and every OPF `<meta>` refinement. Multiple values on the same DC key (e.g. two `dc:creator` elements) are preserved in `ExtractedMetadata` as an array.
- **ORA (Open Raster)** files are now routed to a dedicated strip path (via the ZIP `mimetype` entry) that blanks the `name` and `description` attributes on every element in `stack.xml` (image, stack, layer, mask, text). Structural attributes (`w`, `h`, `x`, `y`, `opacity`, `src`, `mask-src`, `composite-op`, `visibility`) are preserved so the image still renders correctly.
- **DPX / CIN** `dpx:*` and `cin:*` per-image production attributes (film title, origination device, file filename, …) are now explicitly removed after `Strip()`; the values are captured in `ExtractedMetadata` under the `dpx` / `cin` keys before being cleared.
- **OOXML embedded thumbnail** (`docProps/thumbnail.{jpeg,png,emf,wmf,gif,tiff}`) is now removed from DOCX/XLSX/PPTX outputs, along with its `_rels/.rels` thumbnail relationship. Prevents a rendered page preview from reaching a vision model.
- **Audio / video regression coverage extended** to every format the `DetectCategory` routes through the media pipeline. New tests cover **M4A** (iTunes audio), **MOV** (QuickTime), **WebM** (EBML container), **WMA** and **WMV** (ASF), **3GP** (`3gp4` brand), **3G2** (`3g2a` brand), **M4V** (Apple iTunes video), **M4B** (Apple audiobook), and **Ogg Opus** (OggS + OpusHead). Each format is verified to be detected (`IsPassthrough = false`), to survive the strip pipeline without throwing, and to preserve its container magic bytes / major brand in the output.

## Changed

- **ODF template / drawing / chart / formula / database / image variant test coverage.** Added nine dedicated `[Fact]` tests to `FileMetadataStripping.Tests/Documents/OdfTests.cs` (mirrored byte-identically into `FileMetadataStripping.O11.Tests/Documents/OdfTests.cs`) — one per ODF variant: `.ott`, `.ots`, `.otp`, `.odg`, `.otg`, `.odc`, `.odf`, `.odb`, `.odi`. Each test constructs a minimal ODF ZIP package with the variant's mimetype (e.g. `application/vnd.oasis.opendocument.text-template`, `application/vnd.oasis.opendocument.graphics`, `application/vnd.oasis.opendocument.chart`) via a new `CreateOdfVariant(mimetype, creator, title, userDefined)` helper in `TestHelpers.Documents.Odf.cs`; the existing `CreateOdt` helper was refactored to delegate to it (behaviour unchanged). Each test verifies the `dc:creator` core property is cleared and captured in `ExtractedMetadata` — proving the same ODF strip path already handled by `.odt` / `.ods` / `.odp` also correctly handles every template / drawing / chart / formula / database / image sibling routed through `IsOdfFormat`'s `application/vnd.oasis.opendocument.*` prefix match. Zero production code changes. Test suite grew from 711 → 720 per platform (1,422 → 1,440 total). Closes the last remaining urgent-priority row (P3 — ODF template / drawing variants) in `docs/format-coverage.md`.

- **OOXML template / macro-enabled variant test coverage.** Added eleven dedicated `[Fact]` tests to `FileMetadataStripping.Tests/Documents/OpenXmlTests.cs` (mirrored byte-identically into `FileMetadataStripping.O11.Tests/Documents/OpenXmlTests.cs`) — one per OOXML variant: `.docm`, `.dotx`, `.dotm`, `.xlsm`, `.xltx`, `.xltm`, `.pptm`, `.potx`, `.potm`, `.ppsx`, `.ppsm`. Each test constructs a minimal OOXML package with the variant's main part URI and content type via a new `CreateOoxmlVariant(mainPartUri, mainContentType, creator)` helper in `TestHelpers.Documents.OpenXml.cs`, then verifies the `dc:creator` core property is cleared and captured in `ExtractedMetadata` — proving the same OOXML strip path already handled by DOCX / XLSX / PPTX also correctly handles every template and macro-enabled sibling. Zero production code changes. Test suite grew from 700 → 711 per platform (1,400 → 1,422 total). Closes urgent-priority row P3 in `docs/format-coverage.md`.

- **Documents partial split by pipeline.** `FileMetadataStripping.Documents.cs` (~1400–1560 lines per platform) was split into seven pipeline-grouped partial-class files following the naming convention `<Base>.Documents.<Pipeline>.cs` where `Pipeline ∈ { Pdf, OpenXml, LegacyOffice, Rtf, Odf, Epub, Ora }`. WordML lives in the `OpenXml` partial (both are Microsoft Word document surfaces); Flat ODF lives in the `Odf` partial (both are OpenDocument variants sharing the same `meta.xml` schema); the shared `StartsWithXmlAngleBracket` helper lives in the `Odf` partial (first user was Flat ODF; WordML calls it cross-partial). The same split was mirrored across all three projects — ODC (`FileMetadataStripping/`), O11 (`FileMetadataStripping.O11/Actions/`), and XIF (`xif/FileMetadataStripping/Source/NET/`) — and to the test infrastructure: `TestHelpers.Documents.cs` (~830 lines) was split into seven matching partials in both `FileMetadataStripping.Tests/` and `FileMetadataStripping.O11.Tests/`. Pure file-layout refactor: no behavior changes, no format changes, no new NuGet dependencies, no interface changes; all 700 tests pass on both platforms.

- ZIP-based file dispatch was refactored: `DetectZipCategory()` reads the archive's `mimetype` entry once and routes ODF, EPUB, and ORA to their dedicated paths, falling back to `FileCategory.OpenXml` for anything else. `IsOdfFormat()` now delegates to the shared `ReadZipMimetype()` helper.

- HEIC, HEIF, and AVIF files are no longer mis-routed to TagLibSharp as audio/video; they are routed to the image pipeline via the new ISOBMFF brand check.
- `StripImageMetadata` now handles `MagickException` on read gracefully: corrupt or undecodable images are returned unchanged with `processingError` in `ExtractedMetadata` instead of raising an exception.
- `StripImageMetadata` now handles `MagickMissingDelegateErrorException` on write gracefully: when Magick.NET cannot re-encode the image to its original format (e.g. HEIC — confirmed absent on both Windows and ODC Amazon Linux 2023 because the x265 HEVC encoder is GPL-licensed and cannot be bundled in redistributable NuGet packages), the clean stripped pixels are transcoded to JPEG instead. `ExtractedMetadata` includes a `transcodedFormat` key explaining the format change and its cause. The original format is not preserved.
- **DIB (Windows Device Independent Bitmap)** is now routed to passthrough alongside BMP, WBMP, XBM, and XPM. DIB has no metadata containers, so the previous Magick.NET decode-and-re-encode round-trip was a pure no-op that added latency without any security benefit. Detection uses a new `IsDibFile()` heuristic that validates the `BITMAPINFOHEADER` structure (header size in {40, 52, 56, 108, 124}, planes = 1, and a valid bit depth of 1/4/8/16/24/32) to avoid false positives on arbitrary binary input.

## Fixed

- **`StripMediaMetadata` no longer crashes on malformed ASF/MP4 input.** TagLibSharp's ASF (WMA / WMV) and MP4 (M4A) parsers throw `NullReferenceException`, `IndexOutOfRangeException`, `EndOfStreamException`, `ArgumentException`, and `OverflowException` on crafted / truncated headers; the previous catch clause only handled `UnsupportedFormatException`, `CorruptFileException`, `ArgumentOutOfRangeException`, and `InvalidOperationException`, so a malformed WMA/WMV or M4A file would propagate the exception and crash the calling process. All of those exception types are now caught; the original file is returned with a `processingError` audit note.
- TGA files are now reliably detected using a v2 footer check combined with a v1 header heuristic (TGA has no start-of-file magic bytes). Decoding now passes an explicit `MagickFormat.Tga` hint to prevent format misidentification.
- ICO (Windows icon) files are now detected via their 4-byte magic (`00 00 01 00`) and routed through the image pipeline; previously they were undetected and passed through unchanged.
- XCF (GIMP native) files are now detected via `gimp xcf ` magic bytes and routed through the image pipeline; previously they were undetected and passed through unchanged.
- DCM (DICOM medical imaging) files are now detected via the 128-byte preamble followed by `DICM` at offset 128 and routed through the image pipeline; previously they were undetected and passed through unchanged.
- `StripImageMetadata` no longer raises `ArgumentOutOfRangeException` when the Magick.NET codec returns an empty frame collection for an input file; empty collections are now handled gracefully and the original file is returned unchanged.
- Non-`MagickException` codec failures (e.g. `InvalidOperationException` thrown by certain decoders) are now caught; the original file is returned unchanged with a `processingError` key in `ExtractedMetadata`.
- Radiance HDR (`.hdr`) output no longer contains encoder-inserted `# Created by ImageMagick` comment lines. `StripHdrCommentLines()` removes all `#`-prefixed comment lines from HDR output while preserving the mandatory `#?RADIANCE` identification line.
- `ExtractedMetadata` no longer includes ImageMagick encoder artifact comment lines under the `comment` key for any image format.

## Documentation

- Stale HEIC/HEIF description in `docs/platform/odc/forge-description.md` and `docs/platform/o11/forge-description.md` corrected — the row previously stated "original file returned with processingError note", but HEIC now transcodes to JPEG with a `transcodedFormat` key.
- Stale `TIFF EXIF` bullet removed from `docs/platform/odc/limitations.md`; added APNG, SVG, EPUB/ORA, and DPX/CIN known-gap bullets. Body kept under the 1000-char Forge limit (969 chars).
- Added HEIC transcode-to-JPEG paragraph and APNG behaviour paragraph to `docs/platform/odc/documentation.md`; added a "Known gaps in the current release" list at the end of NOTES.
- README updated: fixed broken link to the deleted `01-exif-metadata-stripping.md` (now points at `tasks.md`); added an APNG behaviour note next to the existing TIFF, HDR, and WBMP notes.
- `tasks.md` item 16 clarified to reflect that HEIC now actively transcodes to JPEG (not just graceful handling); added items 17 (format coverage expansion — completed), 18 (DPX/CIN `cin:*` stripping), and 19 (ORA mis-classification).
- Removed `untested-formats.md` (component root) — the file's "Implementation gaps" table listed DPX/CIN, SVG, EPUB, and ORA as unresolved bugs, all now fully implemented per this release, and its role has been fully superseded by `docs/format-coverage.md` (the single source of truth per HANDOFF). Two pieces of unique still-accurate content were folded into `docs/format-coverage.md` before deletion: a new **"How image stripping works"** section documenting the decode → `Strip()` → write pipeline and its three outcomes (round-trip / JPEG fallback / decode error); and a note on the WebP row recording the silent per-frame-metadata loss when the `libwebpmux` delegate is absent from Magick.NET-Q8.
- `docs/format-coverage.md` non-image section restructured into eight per-pipeline subsections (PDF, Office Open XML, Legacy binary Office, RTF, ODF, EPUB, ORA, SVG, Audio, Video), each enumerating every format the parser library is documented to support alongside our detection and test status. Previously the section was a 9-row aggregate table and only enumerated the formats that had a dedicated test — TagLibSharp's full audio/video matrix, PDFsharp's PDF-family variants, DocumentFormat.OpenXml's template/macro variants, and the full ODF mimetype family are now visible in one place, with `✅ / ⚠️ / ❌` status per row. The now-redundant "Audio detection gaps" subsection was removed; its rows are covered in the new Audio / Video pipeline tables.
- Passthrough tests moved from the test project root into a `Passthrough/` subfolder (both `FileMetadataStripping.Tests/Passthrough/PassthroughTests.cs` and `FileMetadataStripping.Tests/Passthrough/HtmlPassthroughTests.cs`, mirrored in `FileMetadataStripping.O11.Tests/Passthrough/`). Aligns with the existing `Images/` / `Documents/` / `Media/` grouping. HANDOFF §1 layout note updated.
- **XIF main-file reconciliation.** `xif/FileMetadataStripping/Source/NET/FileMetadataStripping.cs` had drifted from the O11 main file: it was declared `public class` (non-partial), it had inline duplicate implementations of `StripImageMetadata` / `StripPdfMetadata` / `StripOpenXmlMetadata` / `StripOdfMetadata` / `StripMediaMetadata`, it was missing enum members and dispatch cases for `Svg` / `LegacyOffice` / `Epub` / `Ora`, and it took no `stripBodyAuthors` parameter. The three sibling partial files (`.Images.cs` / `.Documents.cs` / `.Media.cs`) were not even referenced by the XIF csproj so their up-to-date implementations were dead code. Fixed in this release:
  - `FileMetadataStripping.cs` rewritten as a proper thin dispatcher (`public partial class CssFileMetadataStripping : IssFileMetadataStripping`) with the full 11-member `FileCategory` enum, the current `MssStripFileMetadata(byte[], out …)` entry point, a complete `DetectCategory` matching O11 (CFBF / BMP / DIB / SVG / ZIP mimetype dispatch / HEIC+AVIF brand check / extended audio / TGA v1+v2 / ICO / XCF / DCM), and the `DetectZipCategory` + `ReadZipMimetype` helpers. Went from ~945 lines to 246. Inline duplicate strip methods removed — the partial files now provide them.
  - `FileMetadataStripping.csproj` extended with `<Compile Include="FileMetadataStripping.Images.cs" />`, `.Documents.cs`, `.Media.cs`, plus `<Reference Include="OpenMcdf">` and `<Reference Include="OpenMcdf.Ole">` pointing at the DLLs already present under `bin/`.
  - `FileMetadataStripping.Images.cs` partial gained the `IsDibFile` helper (BITMAPINFOHEADER heuristic) so the main dispatcher's BMP/DIB passthrough branch resolves.
  - `StripBodyAuthors` was subsequently exposed on the XIF by opening the extension in Integration Studio, adding a `Boolean` input parameter to the `StripFileMetadata` Server Action (not mandatory, default `False`, description matching the ODC `[OSParameter]` text), and re-running Update Source Code. The regenerated `Interface.cs` now declares `MssStripFileMetadata(byte[] ssRawFile, bool ssStripBodyAuthors, out RCFileMetadataResultRecord ssStripFileMetadata)`; the XIF `FileMetadataStripping.cs` dispatcher was updated in the same pass to thread `ssStripBodyAuthors` into the `StripOpenXmlMetadata` branch, removing the previous `stripBodyAuthors: false` hardcode and its accompanying `<remarks>` block. The `FileMetadataResult` structure descriptions and the Action-level description were also refreshed inside Integration Studio to match the extended supported-format list (SVG / EPUB / ORA / CFBF / RTF / extended audio + video).
  - The XIF project now builds cleanly (`dotnet build FileMetadataStripping.csproj` → `Build succeeded. 0 Error(s)`).

## Removed

*(nothing)*
